export default function NavBar({ children }) {
    return (
      <>
        <nav>
          {/* Nav links, icons, content */}
        </nav>
        <div>
          {children}
        </div>
      </>
    );
  }